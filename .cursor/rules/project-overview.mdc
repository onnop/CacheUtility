---
description: 
globs: 
alwaysApply: false
---
# CacheUtility Project Overview

## Project Purpose
CacheUtility is a thread-safe, generic wrapper for `System.Runtime.Caching` that provides advanced caching features for .NET applications. It is distributed as a NuGet package and supports:

- Thread-safe memory caching with lock-free hot paths (`ConcurrentDictionary` + `Lazy<T>` populate dedup)
- Sync (`Get<T>`) and async (`GetAsync<T>`) populate methods, with single-flight de-duplication
- `TryGet<T>` for cache-only lookups (never invokes populate)
- Cache groups with strongly-typed bulk access (`GetAllByGroup<T>`)
- Cycle-safe dependency relationships between groups (`SetDependencies`)
- Multiple expiration strategies (sliding / absolute) and non-blocking auto-refresh
- Optional persistent cache to disk (atomic writes, group whitelist, background cleanup)
- Built-in diagnostic logging via `Microsoft.Extensions.Logging` (`AddCacheLogging()`)

## Key Project Structure

### Core Files
- [Cache.cs](mdc:Cache.cs) - Main implementation of the static `Cache` class (all caching, persistent storage, and metadata logic)
- [CacheServiceExtensions.cs](mdc:CacheServiceExtensions.cs) - DI helper (`AddCacheLogging()`) for `IServiceCollection`
- [CacheUtility.csproj](mdc:CacheUtility.csproj) - Project file with NuGet package configuration
- [README.md](mdc:README.md) - Comprehensive documentation with usage examples
- [CHANGELOG.md](mdc:CHANGELOG.md) - Per-version release notes
- [License.txt](mdc:License.txt) - MIT license

### Test Project
- [CacheUtility.Tests/](mdc:CacheUtility.Tests) - xUnit test project
- [CacheTests.cs](mdc:CacheUtility.Tests/CacheTests.cs) - Original feature tests (Get, RemoveGroup, persistent cache, metadata)
- [BugFixTests.cs](mdc:CacheUtility.Tests/BugFixTests.cs) - Phase 1 correctness regressions (RefreshLock, sliding LastAccess, dependency cycles, etc.)
- [NewApiTests.cs](mdc:CacheUtility.Tests/NewApiTests.cs) - Coverage for `GetAsync`, `GetAllByGroup<T>`, `TryGet<T>`
- [PersistentCacheHardeningTests.cs](mdc:CacheUtility.Tests/PersistentCacheHardeningTests.cs) - Atomic writes, orphan cleanup, sliding-touch throttle
- [TestCollectionConfig.cs](mdc:CacheUtility.Tests/TestCollectionConfig.cs) - Disables xUnit parallelization (the `Cache` class is static)

### Build & Packaging
- Target framework: .NET 9.0
- Dependencies: `System.Runtime.Caching`, `Microsoft.Extensions.Logging.Abstractions`, `Microsoft.Extensions.Hosting.Abstractions`, `System.Text.Json`
- Configured for NuGet package generation on build (`GeneratePackageOnBuild`)
- Current version: see `<Version>` in [CacheUtility.csproj](mdc:CacheUtility.csproj) (1.4.0 at the time of writing)

## Main Class: Cache
The `Cache` class is `static` (not abstract — it cannot be instantiated). Public surface:

- `Get<TData>(...)` / `GetAsync<TData>(...)` — retrieve or populate, with sync or async populate methods
- `TryGet<TData>(key, group, out value)` — cache-only lookup, never populates
- `Remove(key, group)`, `Remove(List<string>, group)`, `RemoveGroup(params string[])`, `RemoveAllButThese(...)`, `RemoveAll()`
- `GetAllByGroup(group)` and `GetAllByGroup<TData>(group)` — bulk retrieval
- `SetDependencies(group, params dependents)` — cascade-on-remove relationships
- `EnablePersistentCache(...)`, `DisablePersistentCache()`, `IsPersistentCacheEnabled`, `GetPersistentCacheOptions()`, `GetPersistentCacheStatistics()`, `CleanupExpiredPersistentCache()`
- `GetAllCacheMetadata()` — diagnostic snapshot of every cached item
- `ConfigureLogging(ILoggerFactory)` — wire up logging in non-DI scenarios

Internally, every cached value is wrapped in `Cache.CacheItem<T>` (which implements an internal `ICacheItem` interface, used to avoid reflection on hot paths like metadata enumeration and size estimation).
